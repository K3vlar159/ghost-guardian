extends Node

signal on_hit(node : Node, knockback_direction)
signal attack_direction_changed(facing_right:bool)

signal blink()

signal heal()

signal startBoss()

signal E1attack_direction_changed(facing_right:bool)

signal lock()

signal unlock()

signal boss_died()


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
